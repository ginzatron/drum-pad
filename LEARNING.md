# Drum Pad — Learning Notes

## TypeScript

- Add `"DOM"` to `compilerOptions.lib` in `tsconfig.json` so TS knows about browser APIs like `document`, `AudioContext`, `window`, etc. Without it, TS only knows about pure JS types — it has no idea the browser exists.

## Web Audio API

## JavaScript Concepts

## CSS / DOM
